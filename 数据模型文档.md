# SRM 寻源项目管理系统 - 数据模型文档（P1 范围）

**版本**: v1.0
**创建日期**: 2026-03-20
**功能分支**: `001-srm-sourcing-fullspec`
**状态**: 草稿

---

## 全局约定

### 命名规范

- 表名前缀: `srm_{module_abbr}_{entity}`
- 模块缩写对照:

| 模块 | 缩写 | 说明 |
| --- | --- | --- |
| 登录与认证 | `auth` | 模块零 |
| 采购需求 | `proc` | 模块一 |
| 寻源项目 | `src` | 模块二 |
| 招投标 | `bid` | 模块三 |
| 报价与竞价 | `quot` | 模块四 |
| 技术方案 | `tech` | 模块六 |
| 供应商管理 | `sup` | 模块十 |
| 工作流 | `wf` | 跨模块 |
| 文件附件 | `file` | 跨模块 |
| 通知 | `notif` | 跨模块 |
| 审计日志 | `audit` | 跨模块 |

### 公共字段

所有表均包含以下公共字段（后续各表定义中省略）:

| 字段名 | 类型 | 空 | 默认值 | 说明 |
| --- | --- | --- | --- | --- |
| `id` | UUID | NOT NULL | `gen_random_uuid()` | 主键 |
| `created_at` | TIMESTAMP | NOT NULL | `CURRENT_TIMESTAMP` | 创建时间 |
| `updated_at` | TIMESTAMP | NOT NULL | `CURRENT_TIMESTAMP` | 更新时间 |
| `created_by` | UUID | NULL | - | 创建人 FK → `srm_auth_user.id` |
| `updated_by` | UUID | NULL | - | 更新人 FK → `srm_auth_user.id` |
| `deleted` | BOOLEAN | NOT NULL | `false` | 软删除标记 |
| `deleted_at` | TIMESTAMP | NULL | - | 软删除时间 |

### 类型约定

- 枚举值: 存储为 `VARCHAR(50)`，代码中使用 Enum 类映射
- 金额: `DECIMAL(18,2)`
- 百分比/权重: `DECIMAL(5,2)`
- 长文本: `TEXT`
- 短文本: `VARCHAR(n)`，默认 `VARCHAR(200)`

---

## 模块零：登录与认证 (auth)

### 1. srm_auth_user — 用户

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `username` | VARCHAR(100) | NOT NULL | UNIQUE | 登录用户名 |
| `password_hash` | VARCHAR(255) | NOT NULL | - | 密码哈希(bcrypt) |
| `email` | VARCHAR(200) | NULL | UNIQUE | 邮箱，供应商用户同时作为登录名 |
| `phone` | VARCHAR(20) | NULL | - | 手机号 |
| `real_name` | VARCHAR(100) | NULL | - | 真实姓名 |
| `user_type` | VARCHAR(50) | NOT NULL | - | 用户类型: `INTERNAL`(采购方) / `SUPPLIER`(供应商) |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'ACTIVE'` | 状态: `ACTIVE`/`LOCKED`/`DISABLED` |
| `department` | VARCHAR(200) | NULL | - | 所属部门 |
| `business_entity` | VARCHAR(200) | NULL | - | 业务实体 |
| `supplier_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 关联供应商（供应商用户专用） |
| `locked_at` | TIMESTAMP | NULL | - | 锁定时间 |
| `lock_expires_at` | TIMESTAMP | NULL | - | 锁定过期时间（30分钟后） |
| `last_login_at` | TIMESTAMP | NULL | - | 最后登录时间 |
| `password_changed_at` | TIMESTAMP | NULL | - | 最后密码修改时间 |
| `remember_token` | VARCHAR(255) | NULL | - | "记住我"令牌 |
| `remember_expires_at` | TIMESTAMP | NULL | - | "记住我"过期时间（7天） |

**状态机:**

```
ACTIVE → LOCKED (连续5次登录失败)
LOCKED → ACTIVE (30分钟自动解锁 或 管理员手动解锁)
ACTIVE → DISABLED (管理员停用)
DISABLED → ACTIVE (管理员启用)
```

**索引:**

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| `uk_user_username` | `username` | UNIQUE | 用户名唯一 |
| `uk_user_email` | `email` | UNIQUE | 邮箱唯一(允许NULL) |
| `idx_user_status` | `status` | BTREE | 状态查询 |
| `idx_user_type` | `user_type` | BTREE | 类型筛选 |
| `idx_user_supplier` | `supplier_id` | BTREE | 供应商关联查询 |

---

### 2. srm_auth_role — 角色

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `role_code` | VARCHAR(50) | NOT NULL | UNIQUE | 角色编码 |
| `role_name` | VARCHAR(100) | NOT NULL | - | 角色名称 |
| `description` | VARCHAR(500) | NULL | - | 角色描述 |
| `is_system` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否系统预置角色 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_role_code` | `role_code` | UNIQUE |

---

### 3. srm_auth_user_role — 用户角色关联 (N:M)

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `user_id` | UUID | NOT NULL | FK → `srm_auth_user.id` | 用户ID |
| `role_id` | UUID | NOT NULL | FK → `srm_auth_role.id` | 角色ID |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_user_role` | `(user_id, role_id)` | UNIQUE |
| `idx_ur_role` | `role_id` | BTREE |

---

### 4. srm_auth_permission — 权限

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `permission_code` | VARCHAR(100) | NOT NULL | UNIQUE | 权限编码 |
| `permission_name` | VARCHAR(200) | NOT NULL | - | 权限名称 |
| `permission_type` | VARCHAR(50) | NOT NULL | - | 类型: `MENU`(菜单) / `ACTION`(操作) |
| `parent_id` | UUID | NULL | FK → self | 父权限(菜单层级) |
| `resource_url` | VARCHAR(500) | NULL | - | 资源路径 |
| `sort_order` | INT | NOT NULL | DEFAULT `0` | 排序序号 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_perm_code` | `permission_code` | UNIQUE |
| `idx_perm_parent` | `parent_id` | BTREE |
| `idx_perm_type` | `permission_type` | BTREE |

---

### 5. srm_auth_role_permission — 角色权限关联 (N:M)

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `role_id` | UUID | NOT NULL | FK → `srm_auth_role.id` | 角色ID |
| `permission_id` | UUID | NOT NULL | FK → `srm_auth_permission.id` | 权限ID |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_role_perm` | `(role_id, permission_id)` | UNIQUE |

---

### 6. srm_auth_login_attempt — 登录尝试记录

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `username` | VARCHAR(100) | NOT NULL | - | 登录用户名 |
| `ip_address` | VARCHAR(45) | NOT NULL | - | 登录IP |
| `user_agent` | VARCHAR(500) | NULL | - | 浏览器UA |
| `success` | BOOLEAN | NOT NULL | - | 是否成功 |
| `failure_reason` | VARCHAR(200) | NULL | - | 失败原因 |
| `attempted_at` | TIMESTAMP | NOT NULL | DEFAULT `CURRENT_TIMESTAMP` | 尝试时间 |

**索引:**

| 索引名 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| `idx_login_username` | `username` | BTREE | 按用户名查询连续失败次数 |
| `idx_login_time` | `attempted_at` | BTREE | 按时间清理 |

---

## 模块一：采购需求管理 (proc)

### 7. srm_proc_purchase_request — 采购申请

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `request_no` | VARCHAR(50) | NOT NULL | UNIQUE | 申请编号, 格式: `PR-XXXX` |
| `business_entity` | VARCHAR(100) | NULL | - | 业务实体: 华南制造/华东研发/集团总部/北方IT |
| `department` | VARCHAR(200) | NULL | - | 申请部门 |
| `applicant_name` | VARCHAR(100) | NULL | - | 申请人 |
| `applicant_id` | UUID | NULL | FK → `srm_auth_user.id` | 申请人用户ID |
| `apply_date` | DATE | NULL | - | 申请日期 |
| `source_system` | VARCHAR(50) | NULL | - | 来源系统: `SRM_MANUAL`/`ERP_SYNC`/`MES_PUSH` |
| `purchase_category` | VARCHAR(100) | NULL | - | 采购品类: 生产设备/原材料/IT设备/办公用品/服务采购 |
| `purchase_method` | VARCHAR(50) | NOT NULL | - | 采购方式: `INQUIRY`(询比价)/`BIDDING`(招投标)/`DIRECT`(直接采购) |
| `total_amount` | DECIMAL(18,2) | NULL | - | 申请金额 |
| `currency` | VARCHAR(10) | NOT NULL | - | 本地币种: `CNY`/`USD`/`EUR` |
| `description` | TEXT | NULL | - | 需求说明 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'DRAFT'` | 状态: `DRAFT`/`PENDING_APPROVAL`/`APPROVED`/`REJECTED`/`WITHDRAWN` |

**状态机:**

```
DRAFT → PENDING_APPROVAL (提交审批)
PENDING_APPROVAL → APPROVED (审批通过)
PENDING_APPROVAL → REJECTED (审批拒绝)
PENDING_APPROVAL → WITHDRAWN (申请人撤回)
REJECTED → DRAFT (重新编辑)
WITHDRAWN → DRAFT (重新编辑)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_pr_no` | `request_no` | UNIQUE |
| `idx_pr_status` | `status` | BTREE |
| `idx_pr_applicant` | `applicant_id` | BTREE |
| `idx_pr_dept` | `department` | BTREE |
| `idx_pr_date` | `apply_date` | BTREE |
| `idx_pr_created` | `created_at` | BTREE |

---

### 8. srm_proc_purchase_request_item — 采购申请明细行

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `request_id` | UUID | NOT NULL | FK → `srm_proc_purchase_request.id` | 所属采购申请 |
| `line_no` | INT | NOT NULL | - | 行号(自动序号) |
| `material_code` | VARCHAR(100) | NULL | - | 物料号 |
| `material_name` | VARCHAR(200) | NOT NULL | - | 物料名称 |
| `specification` | VARCHAR(500) | NULL | - | 规格型号 |
| `quantity` | DECIMAL(18,4) | NOT NULL | CHECK `> 0` | 数量 |
| `unit` | VARCHAR(50) | NULL | - | 单位 |
| `purchase_method` | VARCHAR(50) | NULL | - | 行级采购方式 |
| `delivery_location` | VARCHAR(500) | NULL | - | 收货地点 |
| `budget_amount` | DECIMAL(18,2) | NULL | - | 预算金额 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_pri_request` | `request_id` | BTREE |
| `uk_pri_line` | `(request_id, line_no)` | UNIQUE |

---

### 9. srm_proc_demand — 采购需求(需求池)

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `demand_no` | VARCHAR(50) | NOT NULL | UNIQUE | 需求编号 |
| `request_item_id` | UUID | NULL | FK → `srm_proc_purchase_request_item.id` | 来源采购申请明细 |
| `request_id` | UUID | NULL | FK → `srm_proc_purchase_request.id` | 来源采购申请 |
| `business_entity` | VARCHAR(100) | NULL | - | 业务实体 |
| `department` | VARCHAR(200) | NULL | - | 需求部门 |
| `material_code` | VARCHAR(100) | NULL | - | 物料号 |
| `material_name` | VARCHAR(200) | NOT NULL | - | 物料名称 |
| `specification` | VARCHAR(500) | NULL | - | 规格型号 |
| `quantity` | DECIMAL(18,4) | NOT NULL | CHECK `> 0` | 数量 |
| `unit` | VARCHAR(50) | NULL | - | 单位 |
| `purchase_method` | VARCHAR(50) | NOT NULL | - | 采购方式 |
| `budget_amount` | DECIMAL(18,2) | NULL | - | 预算金额 |
| `demand_date` | DATE | NULL | - | 需求日期 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待采购)/`LOCKED`(已锁定)/`IN_PROGRESS`(采购中)/`COMPLETED`(已完成)/`CANCELLED`(已取消) |
| `sourcing_project_id` | UUID | NULL | FK → `srm_src_project.id` | 关联寻源项目(锁定后) |
| `parent_demand_id` | UUID | NULL | FK → self | 父需求ID(拆分场景) |
| `merged_from_ids` | TEXT | NULL | - | 合并来源需求ID列表(JSON数组) |

**状态机:**

```
PENDING → LOCKED (被寻源项目引用)
PENDING → CANCELLED (取消)
LOCKED → IN_PROGRESS (项目启动)
IN_PROGRESS → COMPLETED (采购完成)
IN_PROGRESS → CANCELLED (取消)
LOCKED → PENDING (废标释放)
IN_PROGRESS → PENDING (废标释放)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_demand_no` | `demand_no` | UNIQUE |
| `idx_demand_status` | `status` | BTREE |
| `idx_demand_method` | `purchase_method` | BTREE |
| `idx_demand_project` | `sourcing_project_id` | BTREE |
| `idx_demand_date` | `demand_date` | BTREE |

---

## 模块二：寻源项目管理 (src)

### 10. srm_src_project — 寻源项目

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_no` | VARCHAR(50) | NOT NULL | UNIQUE | 项目编号 |
| `project_title` | VARCHAR(500) | NOT NULL | - | 项目标题 |
| `purchase_org` | VARCHAR(100) | NOT NULL | - | 采购组织: 1/2/3部 |
| `country` | VARCHAR(50) | NOT NULL | - | 需求工厂所属国家 |
| `dev_type` | VARCHAR(50) | NOT NULL | - | 开发类型: `SUPPLIER_SELECT`/`TECH_DEV`/`COMMERCIAL_NEGO`/`FRAMEWORK` |
| `biz_type` | VARCHAR(50) | NOT NULL | - | 业务类型: `RD`/`PRODUCTION`/`ENGINEERING`/`IT`/`ADMIN` |
| `subject_type` | VARCHAR(50) | NULL | - | 标的物类型: 设备/原材料/服务/IT硬件/软件 |
| `award_method` | VARCHAR(50) | NOT NULL | - | 决标方式: `BY_ITEM`/`BEST_VALUE`/`LOWEST_PRICE`/`BY_PACKAGE` |
| `evaluation_method` | VARCHAR(50) | NOT NULL | - | 评选方式: `LOWEST_PRICE`/`COMPREHENSIVE`/`TECH_FIRST`/`VALUE_RATIO` |
| `allow_partial_quote` | BOOLEAN | NULL | DEFAULT `false` | 是否允许部分报价 |
| `target_supplier_count` | INT | NOT NULL | CHECK `>= 1` | 拟选定供应商数量 |
| `department` | VARCHAR(200) | NOT NULL | - | 所属部门 |
| `project_manager` | VARCHAR(100) | NOT NULL | - | 项目负责人 |
| `project_manager_id` | UUID | NULL | FK → `srm_auth_user.id` | 项目负责人用户ID |
| `budget` | DECIMAL(18,2) | NOT NULL | CHECK `> 0` | 项目预算 |
| `purchase_method` | VARCHAR(50) | NOT NULL | - | 采购方式(创建后不可更改) |
| `need_tech_review` | BOOLEAN | NOT NULL | DEFAULT `true` | 需要技术评审 |
| `need_auction` | BOOLEAN | NOT NULL | DEFAULT `false` | 需要电子竞价 |
| `show_score_weight` | BOOLEAN | NOT NULL | DEFAULT `true` | 公开评分权重 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'DRAFT'` | 状态: `DRAFT`/`IN_PROGRESS`/`AWARDED`/`CLOSED`/`VOID`/`TERMINATED` |
| `void_reason` | TEXT | NULL | - | 废标原因 |
| `terminate_reason` | TEXT | NULL | - | 终止原因 |

**状态机:**

```
DRAFT → IN_PROGRESS (发布项目, 需审批)
IN_PROGRESS → AWARDED (完成定标)
IN_PROGRESS → VOID (废标)
IN_PROGRESS → TERMINATED (终止)
AWARDED → CLOSED (归档)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_project_no` | `project_no` | UNIQUE |
| `idx_project_status` | `status` | BTREE |
| `idx_project_method` | `purchase_method` | BTREE |
| `idx_project_dept` | `department` | BTREE |
| `idx_project_manager` | `project_manager_id` | BTREE |
| `idx_project_created` | `created_at` | BTREE |

---

### 11. srm_src_project_member — 项目成员

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `user_id` | UUID | NOT NULL | FK → `srm_auth_user.id` | 成员用户 |
| `role_type` | VARCHAR(50) | NOT NULL | - | 角色: `PROJECT_LEAD`/`PROCUREMENT_LEAD`/`TECH_LEAD`/`COMMERCIAL_LEAD` |
| `is_locked` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否锁定(审批中锁定) |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_pm_project_user` | `(project_id, user_id, role_type)` | UNIQUE |
| `idx_pm_project` | `project_id` | BTREE |
| `idx_pm_user` | `user_id` | BTREE |

---

### 12. srm_src_timeline_node — 时间轴节点

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `node_code` | VARCHAR(50) | NOT NULL | - | 节点编码 |
| `node_name` | VARCHAR(100) | NOT NULL | - | 节点名称(立项/发布/报名开始/截止/技术方案开始/截止/报价开始/截止/完成 等) |
| `node_type` | VARCHAR(50) | NOT NULL | - | 类型: `FIXED`(固定)/`OPTIONAL`(可选) |
| `start_time` | TIMESTAMP | NULL | - | 开始时间 |
| `end_time` | TIMESTAMP | NULL | - | 截止时间 |
| `allow_extension` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否允许延期 |
| `sort_order` | INT | NOT NULL | - | 排序序号 |
| `actual_start_time` | TIMESTAMP | NULL | - | 实际开始时间 |
| `actual_end_time` | TIMESTAMP | NULL | - | 实际结束时间 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_tn_project` | `project_id` | BTREE |
| `uk_tn_project_code` | `(project_id, node_code)` | UNIQUE |

---

### 13. srm_src_supplier_invitation — 供应商邀请

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 供应商 |
| `invitation_status` | VARCHAR(50) | NOT NULL | DEFAULT `'SENT'` | 状态: `SENT`/`VIEWED`/`CONFIRMED`/`REGISTERED`/`DECLINED` |
| `invited_at` | TIMESTAMP | NOT NULL | - | 邀请时间 |
| `viewed_at` | TIMESTAMP | NULL | - | 查看时间 |
| `confirmed_at` | TIMESTAMP | NULL | - | 确认时间 |
| `decline_reason` | TEXT | NULL | - | 不参与原因 |
| `contact_name` | VARCHAR(100) | NULL | - | 联系人姓名(报名填写) |
| `contact_phone` | VARCHAR(20) | NULL | - | 联系电话(报名填写) |
| `contact_email` | VARCHAR(200) | NULL | - | 联系邮箱 |
| `participation_status` | VARCHAR(50) | NOT NULL | DEFAULT `'NOT_REGISTERED'` | 参与状态: `NOT_REGISTERED`/`REGISTERED`/`TECH_SUBMITTED`/`PENDING_QUOTE`/`QUOTED`/`NOT_PARTICIPATING` |
| `force_invited` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否强制邀请(D级供应商覆盖) |
| `force_invite_reason` | TEXT | NULL | - | 强制邀请原因 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_si_project_supplier` | `(project_id, supplier_id)` | UNIQUE |
| `idx_si_project` | `project_id` | BTREE |
| `idx_si_supplier` | `supplier_id` | BTREE |
| `idx_si_status` | `invitation_status` | BTREE |

---

### 14. srm_src_clarification — 澄清记录

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 提问供应商 |
| `question_content` | TEXT | NOT NULL | - | 问题描述 |
| `question_time` | TIMESTAMP | NOT NULL | DEFAULT `CURRENT_TIMESTAMP` | 提问时间 |
| `reply_content` | TEXT | NULL | - | 回复内容 |
| `reply_user_id` | UUID | NULL | FK → `srm_auth_user.id` | 回复人 |
| `reply_time` | TIMESTAMP | NULL | - | 回复时间 |
| `is_replied` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否已回复 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_clar_project` | `project_id` | BTREE |
| `idx_clar_supplier` | `supplier_id` | BTREE |
| `idx_clar_replied` | `is_replied` | BTREE |

---

## 模块三：招投标管理 (bid)

### 15. srm_bid_opening — 开标记录

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `opening_no` | VARCHAR(50) | NOT NULL | UNIQUE | 开标编号 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待开标)/`OPENED`(已开标) |
| `opening_time` | TIMESTAMP | NULL | - | 开标时间 |
| `registered_count` | INT | NOT NULL | DEFAULT `0` | 报名供应商数 |
| `quoted_count` | INT | NOT NULL | DEFAULT `0` | 报价供应商数 |
| `min_quote` | DECIMAL(18,2) | NULL | - | 最低报价 |
| `max_quote` | DECIMAL(18,2) | NULL | - | 最高报价 |

**状态机:**

```
PENDING → OPENED (开标, 不可逆)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_bo_no` | `opening_no` | UNIQUE |
| `idx_bo_project` | `project_id` | BTREE |
| `idx_bo_status` | `status` | BTREE |

---

### 16. srm_bid_evaluation — 评标

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `opening_id` | UUID | NOT NULL | FK → `srm_bid_opening.id` | 关联开标 |
| `evaluation_no` | VARCHAR(50) | NOT NULL | UNIQUE | 评标编号 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待评标)/`IN_PROGRESS`(评标中)/`COMPLETED`(已完成) |
| `tech_weight` | DECIMAL(5,2) | NOT NULL | DEFAULT `60.00` | 技术分权重(%) |
| `commercial_weight` | DECIMAL(5,2) | NOT NULL | DEFAULT `40.00` | 商务分权重(%) |
| `completed_at` | TIMESTAMP | NULL | - | 评标完成时间 |

**状态机:**

```
PENDING → IN_PROGRESS → COMPLETED
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_be_no` | `evaluation_no` | UNIQUE |
| `idx_be_project` | `project_id` | BTREE |
| `idx_be_status` | `status` | BTREE |

---

### 17. srm_bid_evaluation_score — 评标评分

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `evaluation_id` | UUID | NOT NULL | FK → `srm_bid_evaluation.id` | 所属评标 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 供应商 |
| `expert_id` | UUID | NULL | FK → `srm_tech_expert.id` | 评审专家 |
| `score_type` | VARCHAR(50) | NOT NULL | - | 评分类型: `TECH`(技术)/`COMMERCIAL`(商务) |
| `dimension` | VARCHAR(100) | NOT NULL | - | 评分维度(技术先进性/可行性/可靠性/实施方案/服务能力) |
| `dimension_weight` | DECIMAL(5,2) | NOT NULL | - | 维度权重(%) |
| `score` | DECIMAL(5,2) | NOT NULL | CHECK `BETWEEN 0 AND 100` | 得分(0-100) |
| `weighted_score` | DECIMAL(5,2) | NOT NULL | - | 加权得分(自动计算) |
| `comment` | TEXT | NULL | - | 评审意见 |
| `is_returned` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否被打回 |
| `return_reason` | TEXT | NULL | - | 打回原因 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_es_eval` | `evaluation_id` | BTREE |
| `idx_es_supplier` | `supplier_id` | BTREE |
| `idx_es_expert` | `expert_id` | BTREE |
| `uk_es_unique` | `(evaluation_id, supplier_id, expert_id, dimension)` | UNIQUE |

---

### 18. srm_bid_award — 定标

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `evaluation_id` | UUID | NOT NULL | FK → `srm_bid_evaluation.id` | 关联评标 |
| `award_no` | VARCHAR(50) | NOT NULL | UNIQUE | 定标编号 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待定标)/`APPROVAL_PENDING`(审批中)/`AWARDED`(已定标)/`VOID`(废标)/`RE_AWARD`(重新定标)/`REJECTED`(驳回) |
| `first_candidate_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 第一候选人 |
| `first_candidate_score` | DECIMAL(5,2) | NULL | - | 第一候选综合分 |
| `second_candidate_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 第二候选人 |
| `second_candidate_score` | DECIMAL(5,2) | NULL | - | 第二候选综合分 |
| `awarded_supplier_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 最终中标供应商 |
| `awarded_amount` | DECIMAL(18,2) | NULL | - | 中标金额 |
| `awarded_at` | TIMESTAMP | NULL | - | 定标时间 |
| `reject_reason` | TEXT | NULL | - | 驳回原因 |

**状态机:**

```
PENDING → APPROVAL_PENDING (提交审批)
APPROVAL_PENDING → AWARDED (审批通过)
APPROVAL_PENDING → REJECTED (驳回) → PENDING (修改后重新提交)
AWARDED → VOID (废标)
AWARDED → RE_AWARD (重新定标)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_ba_no` | `award_no` | UNIQUE |
| `idx_ba_project` | `project_id` | BTREE |
| `idx_ba_status` | `status` | BTREE |
| `idx_ba_awarded_supplier` | `awarded_supplier_id` | BTREE |

---

### 19. srm_bid_award_notice — 中标通知书

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `award_id` | UUID | NOT NULL | FK → `srm_bid_award.id` | 关联定标 |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 中标供应商 |
| `notice_no` | VARCHAR(50) | NOT NULL | UNIQUE | 通知书编号 |
| `project_name` | VARCHAR(500) | NOT NULL | - | 项目名称 |
| `awarded_amount` | DECIMAL(18,2) | NOT NULL | - | 中标金额 |
| `price_valid_days` | INT | NOT NULL | DEFAULT `90` | 价格有效期(天) |
| `confirm_deadline` | TIMESTAMP | NOT NULL | - | 确认截止时间(默认5工作日) |
| `pdf_file_id` | UUID | NULL | FK → `srm_file_attachment.id` | PDF文件 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'GENERATED'` | 状态: `GENERATED`/`SENT`/`CONFIRMED`/`REJECTED`/`EXPIRED` |
| `sent_at` | TIMESTAMP | NULL | - | 发送时间 |
| `confirmed_at` | TIMESTAMP | NULL | - | 供应商确认时间 |
| `confirmed_by` | VARCHAR(100) | NULL | - | 确认人 |
| `reject_reason` | TEXT | NULL | - | 拒绝原因 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_an_no` | `notice_no` | UNIQUE |
| `idx_an_award` | `award_id` | BTREE |
| `idx_an_supplier` | `supplier_id` | BTREE |
| `idx_an_status` | `status` | BTREE |

---

## 模块四：报价与竞价管理 (quot)

### 20. srm_quot_review — 报价评审

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `review_no` | VARCHAR(50) | NOT NULL | UNIQUE | 评审编号 |
| `round` | INT | NOT NULL | DEFAULT `1` | 轮次(1/2/3) |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待评审)/`IN_PROGRESS`(评审中)/`COMPLETED`(已完成) |
| `recommended_supplier_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 推荐中标供应商 |
| `recommend_reason` | TEXT | NULL | - | 推荐理由(必填) |
| `detailed_opinion` | TEXT | NULL | - | 详细评审意见 |
| `commercial_score` | DECIMAL(5,2) | NULL | - | 商务分(满分100) |
| `tech_score` | DECIMAL(5,2) | NULL | - | 技术分(满分100) |
| `comprehensive_score` | DECIMAL(5,2) | NULL | - | 综合得分(商务40%+技术60%) |

**状态机:**

```
PENDING → IN_PROGRESS → COMPLETED
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_qr_no` | `review_no` | UNIQUE |
| `idx_qr_project` | `project_id` | BTREE |
| `idx_qr_status` | `status` | BTREE |

---

### 21. srm_quot_item — 报价明细

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `review_id` | UUID | NOT NULL | FK → `srm_quot_review.id` | 所属评审 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 报价供应商 |
| `round` | INT | NOT NULL | DEFAULT `1` | 轮次 |
| `material_name` | VARCHAR(200) | NULL | - | 物料名称 |
| `specification` | VARCHAR(500) | NULL | - | 规格型号 |
| `quantity` | DECIMAL(18,4) | NULL | - | 数量 |
| `unit_price_tax_incl` | DECIMAL(18,2) | NULL | - | 含税单价 |
| `discount` | DECIMAL(5,4) | NULL | - | 优惠折扣 |
| `unit_price_discounted` | DECIMAL(18,2) | NULL | - | 折后单价(自动计算) |
| `total_price` | DECIMAL(18,2) | NULL | - | 合价(自动计算) |
| `currency` | VARCHAR(10) | NOT NULL | DEFAULT `'CNY'` | 币种 |
| `tax_rate` | DECIMAL(5,2) | NOT NULL | DEFAULT `13.00` | 税率(%) |
| `quote_status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 报价状态: `PENDING`/`SUBMITTED`/`WITHDRAWN` |
| `submitted_at` | TIMESTAMP | NULL | - | 提交时间 |
| `withdraw_count` | INT | NOT NULL | DEFAULT `0` | 撤回次数(每轮最多3次) |
| `withdraw_reason` | TEXT | NULL | - | 撤回原因 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_qi_review` | `review_id` | BTREE |
| `idx_qi_supplier` | `supplier_id` | BTREE |
| `idx_qi_round` | `round` | BTREE |

---

### 22. srm_quot_auction_session — 竞价场次

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `session_no` | VARCHAR(50) | NOT NULL | UNIQUE | 竞价编号 |
| `auction_type` | VARCHAR(50) | NOT NULL | - | 竞价类型: `ENGLISH`(英式)/`REVERSE`(逆向) |
| `starting_price` | DECIMAL(18,2) | NOT NULL | - | 起始价 |
| `current_lowest_price` | DECIMAL(18,2) | NULL | - | 当前最低价 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'NOT_STARTED'` | 状态: `NOT_STARTED`/`IN_PROGRESS`/`ENDED` |
| `start_time` | TIMESTAMP | NOT NULL | - | 竞价开始时间 |
| `end_time` | TIMESTAMP | NOT NULL | - | 原定结束时间 |
| `actual_end_time` | TIMESTAMP | NULL | - | 实际结束时间(含延时) |
| `extension_count` | INT | NOT NULL | DEFAULT `0` | 已延时次数 |
| `participant_count` | INT | NOT NULL | DEFAULT `0` | 参与供应商数 |
| `rule_id` | UUID | NULL | FK → `srm_quot_auction_rule.id` | 关联竞价规则 |

**状态机:**

```
NOT_STARTED → IN_PROGRESS → ENDED
IN_PROGRESS → ENDED (手动结束/超时自动结束)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_as_no` | `session_no` | UNIQUE |
| `idx_as_project` | `project_id` | BTREE |
| `idx_as_status` | `status` | BTREE |

---

### 23. srm_quot_auction_bid — 竞价出价记录

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `session_id` | UUID | NOT NULL | FK → `srm_quot_auction_session.id` | 所属竞价场次 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 出价供应商 |
| `bid_amount` | DECIMAL(18,2) | NOT NULL | CHECK `> 0` | 出价金额 |
| `bid_time` | TIMESTAMP | NOT NULL | - | 出价时间 |
| `bid_rank` | INT | NULL | - | 出价排名(实时) |
| `triggered_extension` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否触发延时 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_ab_session` | `session_id` | BTREE |
| `idx_ab_supplier` | `supplier_id` | BTREE |
| `idx_ab_time` | `bid_time` | BTREE |

---

### 24. srm_quot_auction_rule — 竞价规则

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NULL | FK → `srm_src_project.id` | 项目级自定义规则(NULL表示系统默认) |
| `min_decrement_amount` | DECIMAL(18,2) | NULL | - | 最小降价幅度(金额, 万元) |
| `min_decrement_pct` | DECIMAL(5,2) | NULL | - | 最小降价幅度(百分比) |
| `min_increment_amount` | DECIMAL(18,2) | NULL | - | 最小加价幅度(金额, 万元) |
| `min_increment_pct` | DECIMAL(5,2) | NULL | - | 最小加价幅度(百分比) |
| `bid_interval_seconds` | INT | NOT NULL | DEFAULT `30` | 出价间隔(秒) |
| `extension_trigger_minutes` | INT | NOT NULL | DEFAULT `5` | 延时触发窗口(分钟) |
| `extension_duration_minutes` | INT | NOT NULL | DEFAULT `5` | 延时时长(分钟) |
| `max_extensions` | INT | NOT NULL | DEFAULT `3` | 最大延时次数 |
| `allow_manual_end` | BOOLEAN | NOT NULL | DEFAULT `true` | 允许手动结束 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_ar_project` | `project_id` | BTREE |

---

## 模块六：技术方案管理 (tech)

### 25. srm_tech_proposal — 技术方案

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 提交供应商 |
| `proposal_no` | VARCHAR(50) | NOT NULL | UNIQUE | 方案编号 |
| `content_json` | TEXT | NULL | - | 方案内容(按模板结构的JSON) |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'DRAFT'` | 状态: `DRAFT`/`SUBMITTED`/`UNDER_REVIEW`/`REVIEW_COMPLETED` |
| `submitted_at` | TIMESTAMP | NULL | - | 提交时间 |
| `is_eliminated` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否被淘汰 |
| `elimination_reason` | TEXT | NULL | - | 淘汰原因 |
| `eliminated_at` | TIMESTAMP | NULL | - | 淘汰时间 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_tp_no` | `proposal_no` | UNIQUE |
| `uk_tp_project_supplier` | `(project_id, supplier_id)` | UNIQUE |
| `idx_tp_project` | `project_id` | BTREE |
| `idx_tp_supplier` | `supplier_id` | BTREE |
| `idx_tp_status` | `status` | BTREE |

---

### 26. srm_tech_review — 技术评审

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `proposal_id` | UUID | NOT NULL | FK → `srm_tech_proposal.id` | 关联技术方案 |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'NOT_REVIEWED'` | 状态: `NOT_REVIEWED`/`PENDING`/`IN_PROGRESS`/`COMPLETED` |
| `average_score` | DECIMAL(5,2) | NULL | - | 专家平均分 |
| `completed_at` | TIMESTAMP | NULL | - | 评审完成时间 |

**状态机:**

```
NOT_REVIEWED → PENDING → IN_PROGRESS → COMPLETED
评分可被打回(需打回原因), 专家重新评审
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_tr_proposal` | `proposal_id` | BTREE |
| `idx_tr_project` | `project_id` | BTREE |
| `idx_tr_status` | `status` | BTREE |

---

### 27. srm_tech_review_score — 技术评审评分

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `review_id` | UUID | NOT NULL | FK → `srm_tech_review.id` | 所属评审 |
| `expert_id` | UUID | NOT NULL | FK → `srm_tech_expert.id` | 评审专家 |
| `dimension` | VARCHAR(100) | NOT NULL | - | 评分维度 |
| `dimension_weight` | DECIMAL(5,2) | NOT NULL | - | 维度权重(%) |
| `score` | DECIMAL(5,2) | NOT NULL | CHECK `BETWEEN 0 AND 100` | 得分(0-100) |
| `weighted_score` | DECIMAL(5,2) | NOT NULL | - | 加权得分 |
| `comment` | TEXT | NULL | - | 评审意见(不少于50字) |
| `is_returned` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否被打回 |
| `return_reason` | TEXT | NULL | - | 打回原因 |
| `scored_at` | TIMESTAMP | NULL | - | 评分时间 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_trs_review` | `review_id` | BTREE |
| `idx_trs_expert` | `expert_id` | BTREE |
| `uk_trs_unique` | `(review_id, expert_id, dimension)` | UNIQUE |

---

### 28. srm_tech_expert — 评审专家

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `expert_name` | VARCHAR(100) | NOT NULL | - | 专家姓名 |
| `gender` | VARCHAR(10) | NOT NULL | - | 性别: `MALE`/`FEMALE` |
| `phone` | VARCHAR(20) | NOT NULL | - | 联系电话 |
| `email` | VARCHAR(200) | NULL | - | 邮箱 |
| `organization` | VARCHAR(200) | NOT NULL | - | 工作单位 |
| `title` | VARCHAR(50) | NOT NULL | - | 职称: `JUNIOR`/`INTERMEDIATE`/`SENIOR`/`SENIOR_HIGH`/`RESEARCHER` |
| `specialty` | VARCHAR(500) | NOT NULL | - | 专业领域 |
| `biography` | TEXT | NULL | - | 专家简介 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'ACTIVE'` | 状态: `ACTIVE`(在库)/`DISABLED`(停用) |
| `overall_rating` | DECIMAL(3,1) | NULL | - | 综合评分(1-5分制) |
| `user_id` | UUID | NULL | FK → `srm_auth_user.id` | 关联系统用户(如有) |

**状态机:**

```
ACTIVE ↔ DISABLED
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_expert_status` | `status` | BTREE |
| `idx_expert_specialty` | `specialty` | BTREE |
| `idx_expert_title` | `title` | BTREE |

---

### 29. srm_tech_expert_certificate — 专家资质证书

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `expert_id` | UUID | NOT NULL | FK → `srm_tech_expert.id` | 所属专家 |
| `cert_name` | VARCHAR(200) | NOT NULL | - | 证书名称 |
| `cert_no` | VARCHAR(100) | NOT NULL | - | 证书编号 |
| `issuing_authority` | VARCHAR(200) | NOT NULL | - | 发证机构 |
| `valid_until` | DATE | NULL | - | 证书有效期 |
| `file_id` | UUID | NULL | FK → `srm_file_attachment.id` | 证书附件 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_ec_expert` | `expert_id` | BTREE |

---

### 30. srm_tech_expert_review_task — 专家评审任务

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `project_id` | UUID | NOT NULL | FK → `srm_src_project.id` | 所属项目 |
| `expert_id` | UUID | NOT NULL | FK → `srm_tech_expert.id` | 评审专家 |
| `assigned_by` | UUID | NOT NULL | FK → `srm_auth_user.id` | 分配人 |
| `assigned_at` | TIMESTAMP | NOT NULL | DEFAULT `CURRENT_TIMESTAMP` | 分配时间 |
| `deadline` | TIMESTAMP | NOT NULL | - | 评审截止时间 |
| `review_note` | TEXT | NULL | - | 评审说明 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'ASSIGNED'` | 状态: `ASSIGNED`/`IN_PROGRESS`/`COMPLETED`/`CANCELLED` |
| `completed_at` | TIMESTAMP | NULL | - | 完成时间 |
| `is_reminded` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否已催办 |
| `reminded_at` | TIMESTAMP | NULL | - | 催办时间 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_ert_project_expert` | `(project_id, expert_id)` | UNIQUE |
| `idx_ert_project` | `project_id` | BTREE |
| `idx_ert_expert` | `expert_id` | BTREE |
| `idx_ert_status` | `status` | BTREE |
| `idx_ert_deadline` | `deadline` | BTREE |

---

## 模块十：供应商管理 (sup)

### 31. srm_sup_supplier — 供应商

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `supplier_name` | VARCHAR(200) | NOT NULL | - | 供应商名称 |
| `credit_code` | VARCHAR(18) | NOT NULL | UNIQUE | 统一社会信用代码(18位) |
| `enterprise_type` | VARCHAR(50) | NOT NULL | - | 企业类型: `LLC`/`JSC`/`SOE`/`FIE` |
| `registered_capital` | DECIMAL(18,2) | NULL | - | 注册资本(万元) |
| `established_date` | DATE | NULL | - | 成立时间 |
| `enterprise_scale` | VARCHAR(50) | NULL | - | 企业规模: `SMALL`/`MEDIUM`/`LARGE` |
| `contact_name` | VARCHAR(100) | NOT NULL | - | 联系人 |
| `contact_phone` | VARCHAR(20) | NOT NULL | - | 联系电话 |
| `email` | VARCHAR(200) | NULL | - | 邮箱 |
| `fax` | VARCHAR(50) | NULL | - | 传真 |
| `address` | VARCHAR(500) | NULL | - | 企业地址 |
| `description` | TEXT | NULL | - | 企业简介 |
| `category` | VARCHAR(100) | NOT NULL | - | 供应商分类: 制造类/服务类/贸易类及子分类 |
| `tag` | VARCHAR(50) | NULL | - | 标签: `STRATEGIC`/`PREFERRED`/`GENERAL` |
| `grade` | VARCHAR(10) | NULL | - | 等级: `A`/`B`/`C` |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(审核中)/`ACTIVE`(正常)/`SUSPENDED`(暂停)/`BLACKLISTED`(黑名单)/`BLACKLIST_RELEASE_PENDING`(申请解除) |
| `compliance_status` | VARCHAR(50) | NOT NULL | DEFAULT `'COMPLIANT'` | 合规状态: `COMPLIANT`(合规)/`EXPIRING`(即将到期)/`NON_COMPLIANT`(不合规) |
| `quality_cert` | VARCHAR(200) | NULL | - | 质量体系认证: ISO9001/ISO14001/IATF16949等 |
| `blacklist_reason` | TEXT | NULL | - | 加入黑名单原因 |
| `blacklist_at` | TIMESTAMP | NULL | - | 加入黑名单时间 |
| `performance_grade` | VARCHAR(10) | NULL | - | 绩效等级: `A`/`B`/`C`/`D`/`F` |
| `performance_score` | DECIMAL(5,2) | NULL | - | 绩效评分(最近) |

**状态机:**

```
PENDING → ACTIVE (审核通过)
ACTIVE → SUSPENDED (暂停)
ACTIVE → BLACKLISTED (加入黑名单) → BLACKLIST_RELEASE_PENDING → ACTIVE (解除)
SUSPENDED → ACTIVE (恢复)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_sup_credit` | `credit_code` | UNIQUE |
| `idx_sup_status` | `status` | BTREE |
| `idx_sup_category` | `category` | BTREE |
| `idx_sup_tag` | `tag` | BTREE |
| `idx_sup_grade` | `grade` | BTREE |
| `idx_sup_compliance` | `compliance_status` | BTREE |
| `idx_sup_perf_grade` | `performance_grade` | BTREE |
| `idx_sup_name` | `supplier_name` | BTREE |

---

### 32. srm_sup_registration — 供应商注册申请

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `supplier_name` | VARCHAR(200) | NOT NULL | - | 供应商名称 |
| `credit_code` | VARCHAR(18) | NOT NULL | - | 统一社会信用代码 |
| `enterprise_type` | VARCHAR(50) | NOT NULL | - | 企业类型 |
| `contact_name` | VARCHAR(100) | NOT NULL | - | 联系人 |
| `contact_phone` | VARCHAR(20) | NOT NULL | - | 联系电话 |
| `email` | VARCHAR(200) | NOT NULL | - | 邮箱(作为登录用户名) |
| `password_hash` | VARCHAR(255) | NOT NULL | - | 密码哈希(注册时供应商自设) |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'PENDING'` | 状态: `PENDING`(待审核)/`APPROVED`(已通过)/`REJECTED`(已拒绝) |
| `reject_reason` | TEXT | NULL | - | 拒绝原因 |
| `reviewed_by` | UUID | NULL | FK → `srm_auth_user.id` | 审核人 |
| `reviewed_at` | TIMESTAMP | NULL | - | 审核时间 |
| `supplier_id` | UUID | NULL | FK → `srm_sup_supplier.id` | 审核通过后关联供应商 |

**状态机:**

```
PENDING → APPROVED (审核通过, 激活账号)
PENDING → REJECTED (审核拒绝, 可重新申请)
```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_sr_credit` | `credit_code` | BTREE |
| `idx_sr_email` | `email` | BTREE |
| `idx_sr_status` | `status` | BTREE |
| `idx_sr_created` | `created_at` | BTREE |

---

### 33. srm_sup_qualification — 供应商资质证书

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `supplier_id` | UUID | NOT NULL | FK → `srm_sup_supplier.id` | 所属供应商 |
| `cert_name` | VARCHAR(200) | NOT NULL | - | 证书名称 |
| `cert_no` | VARCHAR(100) | NULL | - | 证书编号 |
| `cert_type` | VARCHAR(100) | NULL | - | 证书类型(ISO9001/ISO14001/IATF16949等) |
| `issuing_authority` | VARCHAR(200) | NULL | - | 发证机构 |
| `issue_date` | DATE | NULL | - | 发证日期 |
| `valid_until` | DATE | NULL | - | 有效期 |
| `cert_status` | VARCHAR(50) | NOT NULL | DEFAULT `'VALID'` | 状态: `VALID`(有效)/`EXPIRING`(即将到期)/`EXPIRED`(已过期) |
| `file_id` | UUID | NULL | FK → `srm_file_attachment.id` | 证书附件 |
| `last_reminder_at` | TIMESTAMP | NULL | - | 最近一次到期提醒时间 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_sq_supplier` | `supplier_id` | BTREE |
| `idx_sq_valid` | `valid_until` | BTREE |
| `idx_sq_status` | `cert_status` | BTREE |

---

## 跨模块：审计日志 (audit)

### 34. srm_audit_log — 审计日志

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `module` | VARCHAR(50) | NOT NULL | - | 所属模块 |
| `entity_type` | VARCHAR(100) | NOT NULL | - | 实体类型(表名) |
| `entity_id` | UUID | NOT NULL | - | 实体ID |
| `action` | VARCHAR(50) | NOT NULL | - | 操作: `CREATE`/`UPDATE`/`DELETE`/`APPROVE`/`REJECT`/`STATUS_CHANGE` |
| `action_detail` | TEXT | NULL | - | 操作详情/变更内容(JSON) |
| `operator_id` | UUID | NULL | FK → `srm_auth_user.id` | 操作人 |
| `operator_name` | VARCHAR(100) | NULL | - | 操作人姓名 |
| `ip_address` | VARCHAR(45) | NULL | - | 操作IP |
| `operated_at` | TIMESTAMP | NOT NULL | DEFAULT `CURRENT_TIMESTAMP` | 操作时间 |
| `override_reason` | TEXT | NULL | - | 覆盖/强制操作原因(如D级供应商强制邀请) |

> 注意: 审计日志保留周期3个月，超期自动清理。

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_al_entity` | `(entity_type, entity_id)` | BTREE |
| `idx_al_operator` | `operator_id` | BTREE |
| `idx_al_time` | `operated_at` | BTREE |
| `idx_al_module` | `module` | BTREE |
| `idx_al_action` | `action` | BTREE |

---

## 跨模块：文件附件 (file)

### 35. srm_file_attachment — 文件附件

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `file_name` | VARCHAR(500) | NOT NULL | - | 原始文件名 |
| `file_path` | VARCHAR(1000) | NOT NULL | - | MinIO存储路径 |
| `file_size` | BIGINT | NOT NULL | - | 文件大小(字节) |
| `file_type` | VARCHAR(20) | NOT NULL | - | 文件类型(后缀: pdf/jpg/png/docx等) |
| `mime_type` | VARCHAR(100) | NULL | - | MIME类型 |
| `bucket_name` | VARCHAR(100) | NOT NULL | - | MinIO桶名 |
| `biz_type` | VARCHAR(100) | NOT NULL | - | 业务类型(deposit-voucher/tech-proposal/cert等) |
| `biz_id` | UUID | NULL | - | 关联业务实体ID |
| `biz_entity_type` | VARCHAR(100) | NULL | - | 关联业务实体类型 |
| `upload_user_id` | UUID | NULL | FK → `srm_auth_user.id` | 上传人 |
| `sha256_hash` | VARCHAR(64) | NULL | - | SHA-256哈希(档案场景校验) |

> 文件命名规则: `{业务类型}/{业务ID}/{时间戳}_{原始文件名}`
> 支持格式白名单: PDF/JPG/JPEG/PNG/DOCX/XLSX/XLS/ZIP/RAR
> 单文件上限: 20MB(保证金/技术方案), 10MB(资质证书)
> 单记录最大附件数: 10个

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_fa_biz` | `(biz_type, biz_id)` | BTREE |
| `idx_fa_entity` | `biz_entity_type` | BTREE |
| `idx_fa_upload_user` | `upload_user_id` | BTREE |

---

## 跨模块：通知 (notif)

### 36. srm_notif_notification — 通知

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `recipient_id` | UUID | NOT NULL | FK → `srm_auth_user.id` | 接收人 |
| `title` | VARCHAR(500) | NOT NULL | - | 通知标题 |
| `content` | TEXT | NULL | - | 通知内容 |
| `notification_type` | VARCHAR(50) | NOT NULL | - | 类型: `SYSTEM`/`APPROVAL`/`REMINDER`/`INVITATION`/`AUCTION` |
| `channel` | VARCHAR(50) | NOT NULL | DEFAULT `'IN_APP'` | 渠道: `IN_APP`(站内)/`EMAIL`/`SMS` |
| `priority` | VARCHAR(20) | NOT NULL | DEFAULT `'NORMAL'` | 优先级: `URGENT`/`HIGH`/`NORMAL`/`LOW` |
| `is_read` | BOOLEAN | NOT NULL | DEFAULT `false` | 是否已读 |
| `read_at` | TIMESTAMP | NULL | - | 已读时间 |
| `biz_type` | VARCHAR(100) | NULL | - | 关联业务类型 |
| `biz_id` | UUID | NULL | - | 关联业务ID |
| `action_url` | VARCHAR(1000) | NULL | - | 跳转链接 |

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `idx_notif_recipient` | `recipient_id` | BTREE |
| `idx_notif_read` | `(recipient_id, is_read)` | BTREE |
| `idx_notif_type` | `notification_type` | BTREE |
| `idx_notif_created` | `created_at` | BTREE |

---

## 跨模块：工作流 (wf)

### 37. srm_wf_instance — 工作流实例

| 字段名 | 类型 | 空 | 约束 | 说明 |
| --- | --- | --- | --- | --- |
| `process_instance_id` | VARCHAR(100) | NOT NULL | UNIQUE | Camunda流程实例ID |
| `process_definition_key` | VARCHAR(100) | NOT NULL | - | 流程定义Key(如: procurement_approval) |
| `business_key` | VARCHAR(100) | NOT NULL | - | 业务单据编号 |
| `biz_type` | VARCHAR(100) | NOT NULL | - | 业务类型(procurement_request/sourcing_project/bid_award等) |
| `biz_id` | UUID | NOT NULL | - | 业务实体ID |
| `initiator_id` | UUID | NOT NULL | FK → `srm_auth_user.id` | 发起人 |
| `status` | VARCHAR(50) | NOT NULL | DEFAULT `'ACTIVE'` | 状态: `ACTIVE`/`COMPLETED`/`CANCELLED`/`SUSPENDED` |
| `approval_result` | VARCHAR(50) | NULL | - | 审批结果: `APPROVED`/`REJECTED`/`RETURNED` |
| `current_approver_role` | VARCHAR(100) | NULL | - | 当前审批人角色 |
| `current_approver_name` | VARCHAR(100) | NULL | - | 当前审批人 |
| `started_at` | TIMESTAMP | NOT NULL | DEFAULT `CURRENT_TIMESTAMP` | 启动时间 |
| `completed_at` | TIMESTAMP | NULL | - | 完成时间 |
| `last_reminded_at` | TIMESTAMP | NULL | - | 最近催办时间 |
| `approval_comment` | TEXT | NULL | - | 审批意见 |

> Camunda回调消息格式:
> ```json
> {
>   "processInstanceId": "流程实例ID",
>   "businessKey": "业务单据编号",
>   "approvalResult": "APPROVED | REJECTED | RETURNED",
>   "approverName": "审批人姓名",
>   "approverRole": "审批人角色",
>   "approvalComment": "审批意见",
>   "completedAt": "ISO8601时间戳"
> }
> ```

**索引:**

| 索引名 | 字段 | 类型 |
| --- | --- | --- |
| `uk_wf_process` | `process_instance_id` | UNIQUE |
| `idx_wf_biz` | `(biz_type, biz_id)` | BTREE |
| `idx_wf_business_key` | `business_key` | BTREE |
| `idx_wf_status` | `status` | BTREE |
| `idx_wf_initiator` | `initiator_id` | BTREE |

---

## ER 关系汇总

### 模块内关系

#### 模块零：登录与认证

```
User ──N:M──> Role ──N:M──> Permission
User ──1:N──> LoginAttempt
```

#### 模块一：采购需求

```
PurchaseRequest ──1:N──> PurchaseRequestItem
PurchaseRequest ──1:N──> ProcurementDemand (通过 request_id)
PurchaseRequestItem ──1:1──> ProcurementDemand (通过 request_item_id)
ProcurementDemand ──self──> ProcurementDemand (拆分: parent_demand_id)
```

#### 模块二：寻源项目

```
SourcingProject ──1:N──> ProjectMember
SourcingProject ──1:N──> TimelineNode
SourcingProject ──1:N──> SupplierInvitation
SourcingProject ──1:N──> ClarificationRecord
```

#### 模块三：招投标

```
SourcingProject ──1:1──> BidOpening
BidOpening ──1:1──> BidEvaluation
BidEvaluation ──1:N──> EvaluationScore
BidEvaluation ──1:1──> BidAward
BidAward ──1:N──> AwardNotice
```

#### 模块四：报价与竞价

```
SourcingProject ──1:N──> QuotationReview (多轮)
QuotationReview ──1:N──> QuotationItem
SourcingProject ──1:1──> AuctionSession
AuctionSession ──1:N──> AuctionBid
AuctionSession ──N:1──> AuctionRule
```

#### 模块六：技术方案

```
SourcingProject ──1:N──> TechProposal
TechProposal ──1:1──> TechReview
TechReview ──1:N──> TechReviewScore
Expert ──1:N──> ExpertCertificate
SourcingProject ──1:N──> ExpertReviewTask
Expert ──1:N──> ExpertReviewTask
```

#### 模块十：供应商管理

```
Supplier ──1:N──> SupplierQualification
Supplier ──1:N──> SupplierRegistration (通过 supplier_id, 审核通过后关联)
```

### 跨模块关系

下面以表格形式列出所有跨模块的外键引用关系:

| 源表 | 源字段 | 目标表 | 关系说明 |
| --- | --- | --- | --- |
| `srm_auth_user` | `supplier_id` | `srm_sup_supplier` | 供应商用户关联供应商主体 |
| `srm_proc_purchase_request` | `applicant_id` | `srm_auth_user` | 采购申请人 |
| `srm_proc_demand` | `sourcing_project_id` | `srm_src_project` | 需求锁定到寻源项目 |
| `srm_src_project` | `project_manager_id` | `srm_auth_user` | 项目负责人 |
| `srm_src_project_member` | `user_id` | `srm_auth_user` | 项目成员用户 |
| `srm_src_supplier_invitation` | `supplier_id` | `srm_sup_supplier` | 邀请供应商 |
| `srm_src_clarification` | `supplier_id` | `srm_sup_supplier` | 提问供应商 |
| `srm_src_clarification` | `reply_user_id` | `srm_auth_user` | 回复人 |
| `srm_bid_evaluation_score` | `supplier_id` | `srm_sup_supplier` | 被评供应商 |
| `srm_bid_evaluation_score` | `expert_id` | `srm_tech_expert` | 评审专家 |
| `srm_bid_award` | `first_candidate_id` | `srm_sup_supplier` | 第一候选供应商 |
| `srm_bid_award` | `second_candidate_id` | `srm_sup_supplier` | 第二候选供应商 |
| `srm_bid_award` | `awarded_supplier_id` | `srm_sup_supplier` | 中标供应商 |
| `srm_bid_award_notice` | `supplier_id` | `srm_sup_supplier` | 通知书收件供应商 |
| `srm_bid_award_notice` | `pdf_file_id` | `srm_file_attachment` | PDF文件附件 |
| `srm_quot_review` | `recommended_supplier_id` | `srm_sup_supplier` | 推荐供应商 |
| `srm_quot_item` | `supplier_id` | `srm_sup_supplier` | 报价供应商 |
| `srm_quot_auction_bid` | `supplier_id` | `srm_sup_supplier` | 竞价出价供应商 |
| `srm_tech_proposal` | `supplier_id` | `srm_sup_supplier` | 方案提交供应商 |
| `srm_tech_expert` | `user_id` | `srm_auth_user` | 专家系统账号 |
| `srm_tech_expert_review_task` | `assigned_by` | `srm_auth_user` | 任务分配人 |
| `srm_tech_expert_review_task` | `expert_id` | `srm_tech_expert` | 被分配专家 |
| `srm_tech_expert_certificate` | `file_id` | `srm_file_attachment` | 证书附件 |
| `srm_sup_registration` | `reviewed_by` | `srm_auth_user` | 审核人 |
| `srm_sup_registration` | `supplier_id` | `srm_sup_supplier` | 通过后关联 |
| `srm_sup_qualification` | `file_id` | `srm_file_attachment` | 证书附件 |
| `srm_wf_instance` | `initiator_id` | `srm_auth_user` | 流程发起人 |
| `srm_notif_notification` | `recipient_id` | `srm_auth_user` | 通知接收人 |
| `srm_audit_log` | `operator_id` | `srm_auth_user` | 操作人 |
| `srm_file_attachment` | `upload_user_id` | `srm_auth_user` | 上传人 |

### 核心关系链路图 (文本ER)

```
                         ┌──────────────────────────────────────────────────────────────────┐
                         │                         srm_auth_user                            │
                         │  (所有模块的 created_by / updated_by 均指向此表)                     │
                         └──────┬──────────────┬──────────────┬──────────────┬───────────────┘
                                │              │              │              │
                    ┌───────────▼──┐   ┌───────▼─────┐  ┌────▼───────┐  ┌──▼─────────────┐
                    │ srm_auth_role│   │srm_proc_     │  │srm_src_    │  │srm_tech_expert │
                    │              │   │purchase_     │  │project     │  │                │
                    └──────────────┘   │request       │  │            │  └────────────────┘
                                       └──────┬───────┘  └─────┬──────┘         │
                                              │                │                │
                                       ┌──────▼───────┐  ┌────▼──────────┐  ┌──▼──────────────────┐
                                       │srm_proc_     │  │srm_src_       │  │srm_tech_expert_     │
                                       │demand        ├─►│supplier_      │  │review_task          │
                                       │              │  │invitation     │  └─────────────────────┘
                                       └──────────────┘  └───┬──────────┘
                                                             │
                                                     ┌───────▼────────┐
                                                     │srm_sup_supplier│◄──────────────────────┐
                                                     └───┬──────┬─────┘                       │
                                                         │      │                             │
                                              ┌──────────▼┐  ┌──▼──────────────┐   ┌─────────▼──────┐
                                              │srm_sup_   │  │srm_bid_award    │   │srm_quot_       │
                                              │qualifi-   │  │                 │   │auction_bid     │
                                              │cation     │  └─────────────────┘   └────────────────┘
                                              └───────────┘

  跨模块多态关联:
  ┌──────────────────┐     ┌───────────────────┐     ┌──────────────────┐
  │srm_file_attachment│     │srm_audit_log       │     │srm_wf_instance   │
  │(biz_type+biz_id) │     │(entity_type+       │     │(biz_type+biz_id) │
  │  多态关联所有业务  │     │ entity_id)         │     │  关联所有审批场景  │
  └──────────────────┘     └───────────────────┘     └──────────────────┘
```

### 关键业务规则在数据层的映射

| 业务规则 | 数据层实现 |
| --- | --- |
| 采购方式创建后不可更改 | `srm_src_project.purchase_method` 应用层校验(UPDATE时禁止修改) |
| 废标释放需求 | 更新 `srm_proc_demand.status` 从 `LOCKED/IN_PROGRESS` 回 `PENDING`, 清空 `sourcing_project_id` |
| 拆分需求总量=原量 | 应用层校验, `parent_demand_id` 追溯原需求 |
| 综合评分公式 | `srm_bid_evaluation.tech_weight * tech_avg + commercial_weight * commercial_avg` |
| 信用代码唯一 | `srm_sup_supplier.credit_code` UNIQUE约束 + 注册时应用层校验 |
| 报价撤回最多3次/轮 | `srm_quot_item.withdraw_count` CHECK `<= 3` |
| 竞价延时 | `srm_quot_auction_session.extension_count` CHECK `<= max_extensions` |
| 绩效等级联动拦截 | 查询 `srm_sup_supplier.performance_grade`, D级警告/F级拦截 |
| 资质到期预警 | 定时任务扫描 `srm_sup_qualification.valid_until`, 60天阈值 |
| 软删除 | 所有表 `deleted=true, deleted_at=CURRENT_TIMESTAMP` |
| 审计日志3个月清理 | 定时任务清理 `srm_audit_log.operated_at < NOW() - 90 DAYS` |

---

## 表清单汇总

| 序号 | 表名 | 模块 | 说明 |
| --- | --- | --- | --- |
| 1 | `srm_auth_user` | 登录与认证 | 用户 |
| 2 | `srm_auth_role` | 登录与认证 | 角色 |
| 3 | `srm_auth_user_role` | 登录与认证 | 用户角色关联 |
| 4 | `srm_auth_permission` | 登录与认证 | 权限 |
| 5 | `srm_auth_role_permission` | 登录与认证 | 角色权限关联 |
| 6 | `srm_auth_login_attempt` | 登录与认证 | 登录尝试记录 |
| 7 | `srm_proc_purchase_request` | 采购需求 | 采购申请 |
| 8 | `srm_proc_purchase_request_item` | 采购需求 | 采购申请明细行 |
| 9 | `srm_proc_demand` | 采购需求 | 采购需求(需求池) |
| 10 | `srm_src_project` | 寻源项目 | 寻源项目 |
| 11 | `srm_src_project_member` | 寻源项目 | 项目成员 |
| 12 | `srm_src_timeline_node` | 寻源项目 | 时间轴节点 |
| 13 | `srm_src_supplier_invitation` | 寻源项目 | 供应商邀请 |
| 14 | `srm_src_clarification` | 寻源项目 | 澄清记录 |
| 15 | `srm_bid_opening` | 招投标 | 开标记录 |
| 16 | `srm_bid_evaluation` | 招投标 | 评标 |
| 17 | `srm_bid_evaluation_score` | 招投标 | 评标评分 |
| 18 | `srm_bid_award` | 招投标 | 定标 |
| 19 | `srm_bid_award_notice` | 招投标 | 中标通知书 |
| 20 | `srm_quot_review` | 报价与竞价 | 报价评审 |
| 21 | `srm_quot_item` | 报价与竞价 | 报价明细 |
| 22 | `srm_quot_auction_session` | 报价与竞价 | 竞价场次 |
| 23 | `srm_quot_auction_bid` | 报价与竞价 | 竞价出价记录 |
| 24 | `srm_quot_auction_rule` | 报价与竞价 | 竞价规则 |
| 25 | `srm_tech_proposal` | 技术方案 | 技术方案 |
| 26 | `srm_tech_review` | 技术方案 | 技术评审 |
| 27 | `srm_tech_review_score` | 技术方案 | 技术评审评分 |
| 28 | `srm_tech_expert` | 技术方案 | 评审专家 |
| 29 | `srm_tech_expert_certificate` | 技术方案 | 专家资质证书 |
| 30 | `srm_tech_expert_review_task` | 技术方案 | 专家评审任务 |
| 31 | `srm_sup_supplier` | 供应商管理 | 供应商 |
| 32 | `srm_sup_registration` | 供应商管理 | 供应商注册申请 |
| 33 | `srm_sup_qualification` | 供应商管理 | 供应商资质证书 |
| 34 | `srm_audit_log` | 跨模块 | 审计日志 |
| 35 | `srm_file_attachment` | 跨模块 | 文件附件 |
| 36 | `srm_notif_notification` | 跨模块 | 通知 |
| 37 | `srm_wf_instance` | 跨模块 | 工作流实例 |

**共计 37 张表**，覆盖 P1 范围全部 10 个模块及跨模块基础设施。
