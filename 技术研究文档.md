# SRM 寻源系统 — 技术研究文档

> 技术栈：Quarkus 3.23.3 + Java 17 · PostgreSQL 15 · Hibernate ORM with Panache · Camunda 7 (REST callback) · MinIO · React 18 + Ant Design · WebSocket/SSE/Polling · e签宝 API · 企查查 API · Docker + Nginx

---

## 1. JWT 认证方案 (Quarkus)

**决策**：采用 SmallRye JWT 扩展，Access Token (15 min) + Refresh Token (7 d) 双令牌策略；登录锁定通过数据库计数器实现（5 次失败锁定 30 分钟）。

**决策理由**：SmallRye JWT 是 Quarkus 官方集成的 MicroProfile JWT 实现，零额外依赖即可获得 `@RolesAllowed`、`@Claim` 注入等能力，与 CDI 安全体系无缝衔接。双令牌策略在安全性（短 Access Token 窗口）与用户体验（静默刷新）之间取得平衡，刷新端点仅接受 HttpOnly Cookie 传递的 Refresh Token 以防 XSS 窃取。

**备选方案**
- **自定义 JWT Filter**：灵活但需手工处理签名验证、角色映射、Token 解析等，维护成本高且易出安全漏洞。
- **Keycloak / OIDC**：功能完善但引入独立服务，部署复杂度超出本项目单体架构的需求。

---

## 2. RBAC 权限体系 (Quarkus)

**决策**：基于 Panache 实体建模（`SysUser`、`SysRole`、`SysPermission`、`SysUserRole`），使用 `@RolesAllowed` 做接口级鉴权，CDI `SecurityInterceptor` 实现数据行级隔离（按组织/部门过滤）。

**决策理由**：Panache 实体模型让角色-权限关系持久化在 PostgreSQL 中，运行时通过自定义 `SecurityIdentityAugmentor` 将数据库角色注入 Quarkus Security Identity，即可复用标准注解。行级隔离通过 CDI 拦截器在 Repository 查询前自动附加组织 ID 条件，避免业务代码重复编写过滤逻辑。

**备选方案**
- **硬编码角色枚举**：无法满足"管理员可动态配置角色权限"的需求。
- **PostgreSQL RLS (Row-Level Security)**：数据库层隔离粒度精确，但需要为每个连接设置 `SET app.current_org`，与连接池（Agroal）配合存在复杂度。

---

## 3. Camunda 7 REST 集成

**决策**：以 External REST API 方式调用 Camunda 7 引擎（独立部署），业务服务暴露回调端点 (`POST /api/v1/workflow/callback/{processInstanceId}`) 接收任务完成通知；通过 `businessKey` 做流程关联；超时催办采用 Camunda Timer Boundary Event (48h) 触发回调。

**决策理由**：REST API 模式避免在 Quarkus 中嵌入 Camunda 引擎 JAR，减少类路径冲突和内存占用。回调端点设计让业务服务保持无状态，Camunda 侧通过 Connector 或 External Task 主动推送状态变更。48 小时催办利用 BPMN Timer Event 原生能力，无需额外定时任务框架。

**备选方案**
- **嵌入式 Camunda (Spring Boot)**：与 Quarkus CDI 模型不兼容，且嵌入式引擎增加服务启动时间。
- **Camunda 8 (Zeebe)**：云原生架构优秀，但 Camunda 8 社区版功能受限且学习曲线陡峭，团队现有 Camunda 7 经验可直接复用。

---

## 4. MinIO 文件存储集成

**决策**：使用 `quarkus-amazon-s3` 扩展（S3 兼容协议）连接 MinIO；上传采用服务端生成 Presigned URL 由前端直传；下载走后端 Proxy 端点做权限校验；孤儿文件通过 Quarkus Scheduler 每日凌晨扫描清理。

**决策理由**：`quarkus-amazon-s3` 基于 AWS SDK v2，原生支持 S3 协议且与 Quarkus 依赖注入体系集成良好，比直接引入 MinIO Java SDK 更统一。Presigned URL 直传减轻后端带宽压力，同时通过短有效期（10 分钟）控制安全风险。

**备选方案**
- **MinIO Java SDK 直连**：API 更贴近 MinIO 特性，但引入非标准依赖，未来若切换到 AWS S3 或阿里 OSS 需大量改造。
- **所有文件经后端中转**：实现简单但大文件上传会阻塞 I/O 线程，影响服务吞吐。

---

## 5. WebSocket 竞价模块

**决策**：竞价大厅使用 `quarkus-websockets-next` 建立实时双向通道，心跳间隔 30s；不支持 WebSocket 的环境降级为 SSE (Server-Sent Events via RESTEasy Reactive `@Sse`)；极端情况再降级为轮询端点（3s 间隔）。断线自动重连后通过 `GET /api/v1/auction/{id}/sync?since={lastSeq}` 拉取缺失增量。

**决策理由**：`quarkus-websockets-next` 是 Quarkus 3.x 推荐的新一代 WebSocket 扩展，支持 CDI 注入和声明式 `@OnMessage` 处理，比旧版 `quarkus-websockets` 更符合响应式编程模型。三级降级策略确保在各种网络环境下竞价功能可用。Gap Sync 机制保证断线期间的报价数据不丢失。

**备选方案**
- **纯轮询方案**：实现最简单但延迟高（秒级），竞价场景下用户体验差。
- **第三方实时服务 (Socket.IO / Pusher)**：引入外部依赖，增加部署复杂度，且竞价数据敏感不宜经第三方。

---

## 6. Flyway 数据库迁移

**决策**：使用 Quarkus Flyway 扩展，脚本命名 `V{yyyyMMdd}_{seq}__{description}.sql`；表名前缀 `srm_{module}_{entity}`（如 `srm_supplier_info`、`srm_sourcing_project`）；主键统一使用 `UUID`（`gen_random_uuid()`）。

**决策理由**：日期+序号命名避免多人并行开发时版本号冲突。三段式表名前缀清晰标识模块归属，便于后期拆分微服务时按前缀划分数据库 Schema。UUID 主键避免分布式环境下的 ID 冲突，PostgreSQL 原生 `uuid` 类型存储效率优于字符串。

**备选方案**
- **BIGINT 自增主键**：查询性能略优且可读性好，但在未来多实例部署或数据合并场景下存在 ID 冲突风险。
- **Liquibase**：功能类似但 XML/YAML 格式冗长，团队更熟悉纯 SQL 迁移脚本。

---

## 7. 前端状态管理

**决策**：采用 Zustand 做全局状态管理；API 层使用 Axios 实例 + 拦截器实现 JWT 自动刷新（401 时静默调用 Refresh 端点并重放请求）。

**决策理由**：Zustand API 极简（无 Provider 包裹、无 boilerplate），与 React 18 并发特性兼容良好，bundle 体积仅 ~1KB。Ant Design Pro 的数据流方案偏重但项目无需 dva/umi 全家桶，Zustand 足以覆盖用户信息、权限菜单、全局通知等场景。

**备选方案**
- **Redux Toolkit**：生态成熟但模板代码多，对于中等复杂度的 SRM 系统属于过度设计。
- **React Context + useReducer**：无需第三方库，但跨组件频繁更新时性能差（Context 值变更导致全部消费者重渲染）。

---

## 8. 审计日志

**决策**：通过 CDI Interceptor（`@AuditLog` 自定义注解）在业务方法执行前后自动采集操作日志，写入 `srm_sys_audit_log` 表，日志格式为结构化 JSON；保留期限 3 个月，通过 Quarkus Scheduler 每日凌晨执行过期数据清理。

**决策理由**：CDI Interceptor 与业务代码解耦，新增审计点只需添加注解无需修改业务逻辑。结构化 JSON 格式便于后期接入 ELK 等日志分析平台。3 个月保留期在合规要求（可追溯）与存储成本之间取得平衡。

**备选方案**
- **Hibernate Envers**：自动跟踪实体变更历史，但生成大量审计表且查询 API 复杂，不适合"谁在什么时候做了什么操作"的业务审计需求。
- **AOP (AspectJ)**：功能类似 CDI Interceptor，但在 Quarkus 中不如 CDI 拦截器与原生编译兼容。

---

## 9. 邮件通知

**决策**：使用 `quarkus-mailer` 扩展（基于 Vert.x Mail Client），结合 Qute 模板引擎实现模板化邮件；覆盖场景包括：供应商注册确认、审批流节点通知、竞价开始/结束提醒、密码重置。

**决策理由**：`quarkus-mailer` 开箱即用支持异步发送（返回 `Uni<Void>`），不阻塞业务线程。Qute 模板引擎是 Quarkus 原生模板方案，支持类型安全模板和热重载，维护邮件模板高效。

**备选方案**
- **Spring Mail (JavaMailSender)**：需引入 Spring 依赖，与 Quarkus CDI 体系不一致。
- **第三方邮件服务 (SendGrid / 阿里邮件推送)**：增加外部依赖和费用，项目初期 SMTP 直连即可满足量级需求。

---

## 10. 软删除模式

**决策**：使用 Hibernate `@SQLDelete` + `@Where(clause = "deleted = false")` 注解实现软删除；对有唯一约束的字段（如供应商统一社会信用代码）采用组合唯一索引 `UNIQUE(credit_code, deleted_at)`，`deleted_at` 为 `NULL` 表示未删除。

**决策理由**：`@SQLDelete`/`@Where` 对业务代码透明，所有 Panache 查询自动过滤已删除记录，开发者无需手动添加条件。`deleted_at` 参与唯一索引的方案利用了 PostgreSQL 对 `NULL` 值在唯一索引中不互斥的特性——已删除记录的 `deleted_at` 各不相同因此不冲突，未删除记录的 `deleted_at` 为 `NULL` 保证唯一。

**备选方案**
- **Panache 自定义基类覆写查询方法**：灵活但需要所有 Repository 继承自定义基类，遗漏风险高。
- **物理删除 + 归档表**：数据完整性好但实现复杂，需维护归档表结构与主表同步。

---

*文档生成时间：2026-03-20*
