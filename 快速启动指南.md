# SRM 寻源系统 - 快速启动指南
ad
## 1. 环境准备asdasdasdasdadaewqvev
asdadsasd
| 工具 | 版本要求 | 验证命令 |
|------|----------|----------|
| Java (JDK) | 17 | `java -version` |
| Node.js | 18+ | `node -v` |
| Docker | 24+ | `docker -v` |
| Docker Compose | 2.20+ | `docker compose version` |
| Maven | 3.9+ | `mvn -v` |

## 2. 一键启动（Docker Compose）

项目根目录创建 `docker-compose.yml`：

```yaml
version: "3.9"
services:
  postgres:
    image: postgres:15
    container_name: srm-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: srm
      POSTGRES_USER: srm
      POSTGRES_PASSWORD: srm_dev_2026
    volumes:
      - pgdata:/var/lib/postgresql/data

  minio:
    image: minio/minio:latest
    container_name: srm-minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - miniodata:/data

  camunda:
    image: camunda/camunda-bpm-platform:7.21.0
    container_name: srm-camunda
    ports:
      - "8081:8080"
    environment:
      DB_DRIVER: org.postgresql.Driver
      DB_URL: jdbc:postgresql://postgres:5432/srm
      DB_USERNAME: srm
      DB_PASSWORD: srm_dev_2026

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: srm-backend
    ports:
      - "8080:8080"
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/srm
      DB_USER: srm
      DB_PASS: srm_dev_2026
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      CAMUNDA_BASE_URL: http://camunda:8080/engine-rest
      JWT_SECRET: srm-dev-jwt-secret-key-2026
    depends_on:
      - postgres
      - minio
      - camunda

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: srm-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  pgdata:
  miniodata:
```

启动全部服务：

```bash
docker compose up -d
```

停止全部服务：

```bash
docker compose down
```

## 3. 开发模式启动

### 3.1 启动基础设施

仅启动数据库、MinIO、Camunda：

```bash
docker compose up -d postgres minio camunda
```

### 3.2 初始化 MinIO 存储桶

首次启动需创建文件存储桶：

```bash
# 安装 MinIO 客户端（mc），或通过 MinIO Console http://localhost:9001 手动创建
docker run --rm --net=host minio/mc \
  alias set local http://localhost:9000 minioadmin minioadmin && \
  mc mb local/srm-dev
```

### 3.3 后端（Quarkus 开发模式）

```bash
cd backend
./mvnw quarkus:dev
```

- 默认端口：`8080`
- 支持热重载，修改代码后自动编译生效
- 数据库迁移：Flyway 在启动时自动执行 `src/main/resources/db/migration/` 下的 SQL 脚本，无需手动操作

### 3.4 前端（React 开发模式）

```bash
cd frontend
npm install
npm run dev
```

- 默认端口：`3000`
- 开发服务器自动代理 `/api` 请求到后端 `http://localhost:8080`

## 4. 环境变量

| 变量名 | 说明 | 默认值（开发环境） |
|--------|------|---------------------|
| `DB_URL` | PostgreSQL 连接地址 | `jdbc:postgresql://localhost:5432/srm` |
| `DB_USER` | 数据库用户名 | `srm` |
| `DB_PASS` | 数据库密码 | `srm_dev_2026` |
| `MINIO_ENDPOINT` | MinIO 服务地址 | `http://localhost:9000` |
| `MINIO_ACCESS_KEY` | MinIO 访问密钥 | `minioadmin` |
| `MINIO_SECRET_KEY` | MinIO 密钥 | `minioadmin` |
| `CAMUNDA_BASE_URL` | Camunda REST API 地址 | `http://localhost:8081/engine-rest` |
| `JWT_SECRET` | JWT 签名密钥 | `srm-dev-jwt-secret-key-2026` |

> **注意**：生产环境务必更换所有默认密码和密钥。

## 5. 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 系统管理员 | `admin` | `Admin@2026` | 拥有全部权限，可管理用户、角色、系统配置 |
| 采购方用户 | `buyer01` | `Buyer@2026` | 可创建采购需求、发起寻源项目、评标定标 |
| 供应商用户 | `supplier01` | `Supplier@2026` | 可查看招标公告、提交投标文件、参与报价 |

## 6. 关键访问地址

| 服务 | 地址 | 说明 |
|------|------|------|
| 前端应用 | http://localhost:3000 | SRM 系统主界面 |
| 后端 API | http://localhost:8080 | RESTful API，Swagger 文档：`/q/swagger-ui` |
| MinIO 控制台 | http://localhost:9001 | 文件存储管理，账号 `minioadmin/minioadmin` |
| Camunda 工作流 | http://localhost:8081 | 流程引擎管理界面 |

## 7. 验证步骤

系统启动后，执行以下冒烟测试确认功能正常：

1. **登录系统**：浏览器打开 http://localhost:3000，使用 `buyer01 / Buyer@2026` 登录
2. **创建采购需求**：进入「采购需求管理」，点击「新建需求」，填写必填项（需求标题、采购品类、预算金额）
3. **保存草稿**：点击「保存草稿」，确认页面提示保存成功且列表中出现新记录
4. **验证文件上传**：在需求详情中上传一份附件，确认上传成功（验证 MinIO 连通性）
5. **验证后端 API**：访问 http://localhost:8080/q/health，确认返回 `status: UP`

全部通过即表示本地开发环境搭建完成。
